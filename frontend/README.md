# git status
check trạng thái hiện tại
# git checkout --branchname
vào nhánh cần code
# git pull 
nếu behind so vói repo
# code.....

# git add 

# git push

# git rm -rf ./ --force
//Neu thay bat man voi truong nhom
