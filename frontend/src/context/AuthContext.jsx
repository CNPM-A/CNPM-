// // src/context/AuthContext.js
// import { createContext } from 'react';

// // Chỉ tạo và export context - không có component, không có logic
// export const AuthContext = createContext({});
// src/context/AuthContext.js
import { createContext } from 'react';

export const AuthContext = createContext(null);
