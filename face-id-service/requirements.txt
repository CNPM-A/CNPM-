# --- Core Web Framework ---
flask==3.0.3
flask-cors==4.0.1
gunicorn==22.0.0
python-dotenv==1.0.1

# --- CÁC THƯ VIỆN AI (Cài theo thứ tự trong Dockerfile) ---
# Dùng dlib-bin thay vì dlib thường (Bản đã biên dịch sẵn cho Linux)
dlib-bin==19.24.2

# Các phụ thuộc của face_recognition
face_recognition_models==0.3.0
Click>=6.0
Pillow>=5.2.0

# --- AI & Face Recognition ---
face_recognition==1.3.0

# --- Image Processing & Math ---
# QUAN TRỌNG: Giữ numpy < 2 để không lỗi OpenCV
numpy<2.0.0

# Xử lý ảnh & số học
# Bản headless nhẹ cho server
opencv-python-headless==4.9.0.80

# --- System & Helpers (QUAN TRỌNG) ---
# Thêm cái này để sửa lỗi "pkg_resources"
setuptools==69.5.1